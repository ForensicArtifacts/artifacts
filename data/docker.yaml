# Docker artifacts

name:  DockerContainerLogs
doc: Location where stdoutput from containers is logged
sources:
- type: FILE
  attributes: {paths: ['/var/lib/docker/containers/*/*-json.log*']}
labels: [Docker, Logs]
supported_os: [Linux]
